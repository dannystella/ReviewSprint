DROP DATABASE IF EXISTS Goalers;
CREATE DATABASE Goalers;
USE Goalers; 


CREATE TABLE Users (
    id INTEGER AUTO_INCREMENT NOT NULL PRIMARY KEY,
    username VARCHAR(255) UNIQUE,
    passwords VARCHAR(255) UNIQUE
);

CREATE TABLE Goals (
    id INTEGER AUTO_INCREMENT,
    goal VARCHAR(255) NOT NULL UNIQUE,
    descriptions VARCHAR(255) NOT NULL UNIQUE
);